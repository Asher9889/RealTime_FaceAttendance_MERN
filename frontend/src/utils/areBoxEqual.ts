// function areBoxesEqual(prev: Box[], next: Box[]): boolean {
//     if (prev.length !== next.length) return false;
  
//     for (let i = 0; i < prev.length; i++) {
//       const p = prev[i];
//       const n = next[i];
  
//       if (
//         Math.abs(p.x - n.x) > 2 ||
//         Math.abs(p.y - n.y) > 2 ||
//         Math.abs(p.width - n.width) > 2 ||
//         Math.abs(p.height - n.height) > 2
//       ) {
//         return false;
//       }
//     }
//     return true;
//   }
  